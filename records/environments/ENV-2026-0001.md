# ENV-2026-0001：集成/演示服务器基线

- 采集时间：2026-07-30 17:40 CST
- 主机：`121.196.149.55`
- 项目目录：`/home/tianqi/ceo-bp`（计划创建）
- 对外端口：`9006/tcp`
- 采集方式：root 只读命令；未修改现有服务

## 基础资源

| 项目 | 结果 |
|---|---|
| 操作系统 | Alibaba Cloud Linux 4.0.3 (OpenAnolis Edition) |
| 内核/架构 | Linux 6.6.102 / x86_64 |
| CPU | 4 vCPU |
| 内存 | 14 GiB 总量；采集时约 2.5 GiB available；无 Swap |
| 磁盘 | 根卷 197 GiB；约 130 GiB 可用 |
| Docker | 26.1.3 |
| 防火墙 | firewalld running |
| SELinux | Disabled |
| 失败 systemd 单元 | 0 |
| 9006 | 采集时无监听，允许 CEO-BP 独占 |

## 共存约束

服务器已有 Nginx、MySQL/PostgreSQL/Redis、Milvus/MinIO/etcd 及多套 Python、Node、.NET 和 Docker 服务，多个 3000/8000/9000 段端口已被占用。`/home/tianqi/aidp` 已有另一套无 Git 历史的 AIDP 原型，默认计划使用 9004。

CEO-BP 必须：

- 使用独立 `/home/tianqi/ceo-bp`、Compose 项目、网络、镜像和端口；
- 不连接、重启、升级、复用或清理现有容器和数据卷；
- 首版只运行一个不带数据库的 API 容器，内存上限 256 MiB；
- 每次部署前后保存现有容器和监听摘要，确认无非预期变化；
- 不修改全局 Nginx、MySQL、PostgreSQL、Redis、Milvus 和防火墙规则。

## 安全观察

- 登录提示存在待安装安全公告，其中包括重要级别更新；
- 登录提示报告近期有一次失败 SSH 登录；
- 当前以 root 口令管理，凭据不得写入 Git、部署文件或记录；
- SELinux 关闭，需用容器只读文件系统、去 Capabilities 和最小暴露补偿。

系统补丁、SSH 策略、口令轮换和防火墙变更可能影响共享服务，必须单独安排维护窗口，本次不执行。

## 部署后记录

- 部署时间：2026-07-30 约 18:00 CST；
- Git 提交：`87863ef858928827dd02acec40146f97d8123e5e`；
- 镜像：`ceo-bp/platform-api:0.2.0`；Image ID `sha256:8a677536cab0fd6a258ae02869fb9eb78f8cd6b556ace70722034ab10b30f592`；
- 传输归档 SHA-256：`595948f1c848e3a750e283b53352c3235971d0636aee22f0538ca82ffe96e7d8`，加载后已删除临时归档；
- 容器：`ceo-bp-platform-api-1`，healthy；
- 监听：`0.0.0.0:9006 → container:8000`；
- 观测资源：约 40.85 MiB / 256 MiB，1 PID；
- 部署前已有容器名称集哈希与部署后排除 CEO-BP 容器的哈希均为 `55209d8ec34e4d590685d2d118fe020a1790ff9df8adc7911854b2b437b6775f`；
- firewalld 配置未修改，外部健康检查可访问；
- 部署后主机可用内存约 2.46 GiB。

服务器直连 Docker Hub 拉取固定基础镜像时，一个 3.52 MiB 层超过 107 秒无进度后被人工取消；未启动失败容器。随后改用本地已验证的同架构镜像传输并校验加载。部署脚本已增加 `CEO_BP_SKIP_BUILD=1` 离线镜像模式。

## 基线结论

9006 已用于轻量 CEO-BP 集成 API；由于内存压力和共享服务密度，不准部署完整 KWeaver 服务群或新增数据库/向量库。环境在每次部署前仍需重新检查端口和可用内存。

## 0.3.0 变更记录

- 变更时间：2026-07-30 18:48—18:52 CST；
- Git 提交：`f10dc710938cf06e6825dd3478a15aea1d7f64aa`；
- 镜像：`ceo-bp/platform-api:0.3.0`；Image ID `sha256:77b43a8ddcd0256f60e5778d4069074285c9b293b62f0070f87f9e8baeb8ee67`；
- 新增同版本 React/TypeScript 控制台和 `/api/v1/overview`；不新增端口、容器、网络、数据库或卷；
- 变更前约 1965 MiB available；变更后应用约 45.92 MiB / 256 MiB，2 PIDs，healthy；
- 内外各 20/20 总览请求成功；外部最大 0.134864 秒；
- 部署前后容器名称列表一致，只重建 `ceo-bp-platform-api-1`；
- 传输归档 SHA-256 `eb8c531497ea82c5a848d055e180778bad93db1101684b8fbb234b14a0bb9d98`，加载后已从服务器删除；
- `/home/tianqi/aidp`、其他容器、Nginx、数据库、firewalld 和系统包未修改。

## 0.3.1 变更记录

- 变更时间：2026-07-30 19:11—19:14 CST；
- Git 提交：`44c94030a5286f8904096cb189966811aa9dde91`；
- 镜像：`ceo-bp/platform-api:0.3.1`；Image ID `sha256:a70467eadea3ee934696ccceaadedefbdb933163dfa4e3d9c9895a7b046c4976`；
- 控制台删除规划和概念展示，新增真实运行时间、版本和端点；不新增端口、容器、网络、数据库或卷；
- 变更前约 1976 MiB available；容器保持非 root、只读、256 MiB、0.75 CPU、128 PID 和 healthy；
- 主机内外健康检查各 20/20，生产 JS 禁用概念文案扫描通过；
- 服务器 GitHub 访问失败后使用完整 Git bundle fast-forward，代码提交仍与 GitHub 一致；
- 部署前后其他 15 个容器名称一致，只重建 `ceo-bp-platform-api-1`；
- 临时 Git bundle 和镜像归档均已删除，项目 Git 工作区干净；
- `/home/tianqi/aidp`、其他容器、Nginx、数据库、firewalld 和系统包未修改。

## 0.4.0 变更记录

- 变更时间：2026-07-31 10:39—10:43 CST；
- Git 提交：`d9ec18cb5909e4b6e546fd6fc0165ac433f2f9c4`；
- 镜像：`ceo-bp/platform-api:0.4.0`；Image ID `sha256:6daeafd80d6b2f776479174a280ddd236d8a75d2d988f4080036514599e3dc50`；
- 新增用户方案比较工作台和无状态决策分析 API；不新增端口、容器、网络、数据库或卷；
- 变更前约 1874 MiB available，变更后约 1877 MiB available；容器保持非 root、只读、256 MiB、0.75 CPU、128 PID 和 healthy；
- 主机内外健康检查各 20/20；真实分析请求和无效输入拒绝均通过；
- 部署前后其他 15 个容器名称一致，只重建 `ceo-bp-platform-api-1`；
- 临时 Git bundle 和镜像归档均已删除，项目 Git 工作区干净；
- `/home/tianqi/aidp`、其他容器、Nginx、数据库、firewalld 和系统包未修改。
