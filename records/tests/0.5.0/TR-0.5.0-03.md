# TR-0.5.0-03：经营对象类型纵向切片测试报告

- 候选版本：`0.5.0-rc1`（未发布）
- 上游基线：`kweaver-core@b9b35fb245c31660127114c883e91165b42dc8f0`
- 环境：本地 Docker Desktop，隔离项目 `ceobp-kweaver-p2`，仅 `127.0.0.1:19006` 对外
- 执行日期：2026-08-03
- 需求：[REQ-2026-0002](../../requirements/REQ-2026-0002.md)
- 结论：Conditional Go（对象类型切片）；9006 与生产部署 No-Go

## 测试范围

- 官方 SDK 的对象类型列表、创建和稀疏响应回读；
- 独立逻辑目录、Vega 托管数据集和 BKN 对象类型绑定；
- 字段类型、唯一性、代码格式、主键与显示字段校验；
- BKN 创建失败时的数据集补偿清理；
- React 对象列表、动态字段表单、提交状态和错误反馈；
- 真实 API 创建、BKN 重启持久化、浏览器布局与控制台错误检查；
- 无成功结果的资源直连对象构建入口治理。

## 自动化结果

| 套件 | 结果 | 证据摘要 |
|---|---|---|
| 文档校验 | Pass | 75 个 Markdown 文件，内部链接与结构通过 |
| Python Ruff / mypy | Pass | 0 lint、0 strict typing 问题 |
| Python pytest | Pass | 36/36；总覆盖率 95.31% |
| 前端 Biome / TypeScript | Pass | 0 格式、静态与类型问题 |
| 前端 Vitest | Pass | 20/20；语句 91.92%、分支 86.77%、函数 95.77%、行 92.11% |
| 前端生产构建 | Pass | Vite 构建成功；最终制品由 Platform API 镜像托管 |

最终本地候选镜像为 `sha256:a84450e7f203b7acda2d8d6dd44b32b1c7892b52d4d81c917e5fe6e8564e88df`，容器以 `10001:10001`、只读根文件系统运行。

## SDK/Core 契约探测

| 探测 | 实际结果 | 处置 |
|---|---|---|
| 对象类型创建响应 | Core 返回仅含 ID 的数组，SDK 模型其余字段为空 | 防腐层检测稀疏响应后调用 SDK `get` 回读 |
| 显式关键词索引 | SDK 未传 `ignore_above_len`，Core 返回 400 | 本切片不开放索引开关，登记升级约束 |
| 关闭显式索引 | 两字段对象创建、列表、详情成功 | 作为当前准入契约 |
| 资源直连对象全量构建 | Core 返回 `NoneConceptType` 422，不生成索引任务 | 从用户界面移除构建按钮 |

契约探测创建的临时对象和数据集均已删除。

## 真实运行时结果

| 用例 | 实际 | 结果 |
|---|---|---|
| 逻辑目录 | `ceobp_business_catalog` 幂等创建并启用，不复用 `adp_bkn_catalog` | Pass |
| 公开 API 创建 | 在“集团经营分析网络”创建“客户”，返回对象 ID `d9o3clq1336s73cneahg` | Pass |
| 字段持久化 | `customer_id/customer_name/customer_level/annual_revenue/active` 及业务名称、类型完整回读 | Pass |
| 主键与显示字段 | 主键 `customer_id`，显示字段 `customer_name` | Pass |
| 重启恢复 | BKN 重启后同一对象 ID、名称和 5 个字段保持一致 | Pass |
| 网络统计 | 详情返回对象类型数 1 | Pass |
| 端口隔离 | 仅 `127.0.0.1:19006`，未操作 9006 | Pass |

## 浏览器验收

- 1280×720 下页面 `scrollWidth == clientWidth == 1280`，无整页横向溢出；
- 知识网络详情显示对象数 1、真实“客户”和 5 个字段；
- 对象表单提供对象名、字段代码、业务名称、五种字段类型、主键、显示字段、添加/删除字段、取消和创建；
- 详情抽屉宽 560px，对象表单在视口内完整显示；
- 浏览器控制台错误和警告为 0；
- 页面不存在“触发全量构建”按钮，残留构建文案已删除。

## 残余风险与限制

- 对象数据集当前只有模式，没有企业数据实例；
- 对象关系、指标、对象实例导入、编辑/删除和版本发布尚未交付；
- 补偿删除失败当前保留原始业务错误并依赖运维清理，后续需增加可靠清理任务和审计；
- 开发栈关闭认证、授权和审计，尚未执行安全、性能、容量及备份恢复测试；
- 当前结果只批准继续 P3 纵向开发，不批准替换 9006 或生产上线。
